# Ask Protocol

When you are unable to find tool, or don't have permissions to execute it, ask user to perform the
action.

When tool is not installed:

```text
The <tool name> is missing. To install, please run:

<installation instructions>
```

When no permission to run tool:

```text
Please run the command:

<command>
```
